// math- provides the basic maths functions 

let x=3.23;
let y=5.26;

//x=Math.round(x);    //to round the function 

//x=Math.floor(x);    //to show the least floor value of the number 

//x=Math.ceil(x);     //to show the highest value of the number

//x=Math.pow(x,2);        //x is the base value and 2 is the power

//x=Math.sqrt(x);          //find the sqrt of the value 

//x=Math.abs(x);          //to find the absolute value of the number 

// maximum= Math.max(x,y);
// minimum=Math.min(x,y);


console.log(minimum);